// Reverse the string and print it

let name = "Nageshwar";

let reverse = "";
let start = name.length - 1; // loop starts from last index of a string
let end = 0
for (let i = start; i >= end; i--) {
  reverse = reverse + name[i];
}
console.log(reverse); // rawhsegaN