//create a object and inside that object write a function, which should add two number and return them.

let obj = {
  sum: function(a, b) {
    return a + b;
  }
}

let output = obj.sum(2, 3);
console.log(output); // 5
