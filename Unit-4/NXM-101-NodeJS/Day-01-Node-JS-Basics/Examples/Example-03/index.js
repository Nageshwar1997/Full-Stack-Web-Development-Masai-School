// Built in Modules

const os = require("os");

// console.log(os);

console.log("Free Memory of my operating system", os.freemem());

console.log("Version of my Operating system", os.version());

console.log("CPU and memory usage of my operating system", os.cpus());
// console.log("CPU first Core and memory usage of my operating system", os.cpus()[0]);
